name: flashcards
description: Flashcards app for studying. 

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_bloc: ^7.0.0-nullsafety.4

flutter:

  uses-material-design: true

  assets:
    - assets/img/stopwatch.png
    - assets/img/score.png
